const exercises=[{id:'soma',level:1,title:'Soma de dois valores',category:'Variaveis',difficulty:'Facil',description:'Crie uma funcao resolver(a, b) que retorna a soma de dois numeros.',starter:'function resolver(a, b) {\n  // escreva sua solucao\n}',tests:[[[2,3],5],[[-4,10],6],[[0,0],0]],hints:['A funcao recebe duas entradas.','Use o operador +.','Retorne a + b.'],solution:'function resolver(a, b) {\n  return a + b;\n}'},{id:'par',level:2,title:'Numero par?',category:'Condicoes',difficulty:'Facil',description:'Crie resolver(numero) que retorna true quando o numero e par.',starter:'function resolver(numero) {\n  // retorne true ou false\n}',tests:[[[8],true],[[7],false],[[0],true]],hints:['Use o resto da divisao.','O operador % retorna o resto.','Use numero % 2 === 0.'],solution:'function resolver(numero) {\n  return numero % 2 === 0;\n}'},{id:'lista',level:3,title:'Somando a lista',category:'Repeticao',difficulty:'Medio',description:'Crie resolver(numeros) que soma uma lista usando repeticao.',starter:'function resolver(numeros) {\n  let total = 0;\n  // percorra a lista\n  return total;\n}',tests:[[[[1,2,3]],6],[[[-2,5]],3],[[[]],0]],hints:['Comece total em 0.','Use for...of.','Some cada numero a total.'],solution:'function resolver(numeros) {\n  let total = 0;\n  for (const numero of numeros) total += numero;\n  return total;\n}'},{id:'palindromo',level:4,title:'Espelho de palavras',category:'Strings',difficulty:'Medio',description:'Crie resolver(texto) que retorna se o texto e igual ao seu inverso.',starter:'function resolver(texto) {\n  // escreva sua solucao\n}',tests:[[['arara'],true],[['ovo'],true],[['codigo'],false]],hints:['Compare o texto com uma versao transformada.','split, reverse e join ajudam.','Compare texto com texto.split(\'\').reverse().join(\'\').'],solution:'function resolver(texto) {\n  return texto === texto.split(\'\').reverse().join(\'\');\n}'}];const app=document.querySelector('#app');let state={current:null,hints:0,progress:load()};function load(){try{return Object.assign({xp:0,solved:[],attempts:[],hintFree:0},JSON.parse(localStorage.getItem('codegym-progress')))}catch(e){return {xp:0,solved:[],attempts:[],hintFree:0}}}function save(){localStorage.setItem('codegym-progress',JSON.stringify(state.progress))}function esc(v){return String(v).replace(/[&<>]/g,c=>({ '&':'&amp;','<':'&lt;','>':'&gt;'})[c])}function stats(){let p=state.progress,ok=p.attempts.filter(a=>a.passed).length;return {xp:p.xp,level:Math.floor(p.xp/100)+1,solved:p.solved.length,attempts:p.attempts,accuracy:p.attempts.length?Math.round(ok*100/p.attempts.length):0}}function nav(v){document.querySelectorAll('nav button').forEach(b=>b.classList.toggle('active',b.dataset.view===v))}function shell(title,sub,body){let s=stats();return `<div class="title-row"><div><div class="eyebrow">CodeGym / sua academia</div><h1>${title}</h1><p>${sub}</p></div><div class="level">Nivel ${s.level} ? ${s.xp} XP</div></div>${body}`}function home(){nav('home');let s=stats(),e=exercises.find(x=>!state.progress.solved.includes(x.id))||exercises[0];app.innerHTML=shell('Ola, estudante','Seu raciocinio fica mais forte a cada tentativa.',`<div class="cards"><div class="card"><div class="label">Nivel</div><div class="value">${s.level}</div></div><div class="card"><div class="label">Experiencia</div><div class="value">${s.xp} XP</div><div class="bar"><i style="width:${s.xp%100}%"></i></div></div><div class="card"><div class="label">Resolvidos</div><div class="value">${s.solved}</div></div><div class="card"><div class="label">Acerto</div><div class="value">${s.accuracy}%</div></div></div><section class="section"><div class="recommendation"><div class="icon">JS</div><div class="grow"><span class="pill">${e.category} ? ${e.difficulty}</span><h3>${e.title}</h3><p>${e.description}</p></div><button class="primary" onclick="openExercise('${e.id}')">Comecar</button></div></section>`)}function train(){nav('train');app.innerHTML=shell('Modo treino','Escolha um desafio para praticar JavaScript.',`<div class="exercise-list">${exercises.map(e=>`<article class="exercise-card"><div class="icon">JS</div><div class="meta"><span class="pill ${e.difficulty==='Medio'?'medium':''}">${e.category} ? ${e.difficulty}</span><h3>${e.title}</h3><p>${e.description}</p></div><button class="primary" onclick="openExercise('${e.id}')">Treinar</button></article>`).join('')}</div>`)}function openExercise(id){let e=exercises.find(x=>x.id===id);state.current=e;state.hints=0;nav('train');app.innerHTML=`<button class="link-btn" onclick="train()">Voltar para treino</button><div class="workspace" style="margin-top:17px"><section class="section problem"><div class="eyebrow">Nivel ${e.level} ? ${e.category}</div><h1>${e.title}</h1><span class="pill ${e.difficulty==='Medio'?'medium':''}">${e.difficulty}</span><p style="margin-top:16px">${e.description}</p><h3 style="margin-top:23px">Como pensar</h3><p>Identifique entradas e saida. Escreva a funcao resolver. Teste casos simples.</p><button class="secondary" onclick="hint()">Mostrar dica (${state.hints}/3)</button><div id="hints"></div><button id="solutionButton" class="link-btn hidden" onclick="solution()">Ver solucao</button><div id="solution"></div></section><section class="section editor-wrap"><div class="editor-top"><span>JavaScript ? solucao.js</span><span>funcao: resolver</span></div><textarea id="code" class="editor" spellcheck="false">${esc(e.starter)}</textarea><div class="actions"><button class="secondary" onclick="resetCode()">Limpar</button><button class="secondary" onclick="execute(false)">Executar</button><button class="primary" onclick="execute(true)">Enviar solucao</button></div><div class="output" id="output"></div></section></div>`}function resetCode(){document.querySelector('#code').value=state.current.starter}function hint(){if(state.hints===3)return;let e=state.current;state.hints++;document.querySelector('#hints').innerHTML+=`<div class="hint"><b>Dica ${state.hints}:</b> ${e.hints[state.hints-1]}</div>`;if(state.hints===3)document.querySelector('#solutionButton').classList.remove('hidden')}function solution(){document.querySelector('#solution').innerHTML=`<div class="hint"><b>Solucao</b><pre>${esc(state.current.solution)}</pre></div>`}function runner(code,tests){return new Promise(resolve=>{let src=`onmessage=e=>{try{let f=new Function('"use strict";\\n'+e.data.code+'\\n;return typeof resolver==="function"?resolver:null')();if(!f)throw Error("Crie uma funcao chamada resolver.");postMessage({results:e.data.tests.map(t=>{let actual=f(...t[0]);return {args:t[0],expected:t[1],actual:actual,passed:JSON.stringify(actual)===JSON.stringify(t[1])}})})}catch(err){postMessage({error:err.message})}}`;let url=URL.createObjectURL(new Blob([src],{type:'text/javascript'})),w=new Worker(url),done=false,finish=r=>{if(done)return;done=true;w.terminate();URL.revokeObjectURL(url);resolve(r)};w.onmessage=e=>finish(e.data);setTimeout(()=>finish({error:'A execucao excedeu 2 segundos.'}),2000);w.postMessage({code:code,tests:tests})})}async function execute(submit){let out=document.querySelector('#output');out.innerHTML='<div class="result">Executando no navegador...</div>';let r=await runner(document.querySelector('#code').value,state.current.tests);if(r.error){out.innerHTML=`<div class="result fail"><b>Nao foi possivel concluir</b><br>${esc(r.error)}</div>`;return}let passed=r.results.every(x=>x.passed);out.innerHTML=`<div class="result ${passed?'ok':'fail'}"><b>${passed?'Excelente! Todos os testes passaram.':'Ainda ha testes para corrigir.'}</b></div>${r.results.map((x,i)=>`<div class="result ${x.passed?'ok':'fail'}"><b>Teste ${i+1}: ${x.passed?'passou':'falhou'}</b><br><small>Entrada: ${esc(JSON.stringify(x.args))} ? Esperado: ${esc(JSON.stringify(x.expected))} ? Recebido: ${esc(JSON.stringify(x.actual))}</small></div>`).join('')}`;if(submit){let p=state.progress;p.attempts.unshift({at:new Date().toISOString(),passed:passed,category:state.current.category});if(passed&&!p.solved.includes(state.current.id)){p.solved.push(state.current.id);p.xp+=state.hints?10:20;if(!state.hints)p.hintFree++}save()}}function challenges(){nav('challenges');let e=exercises[new Date().getDate()%exercises.length];app.innerHTML=shell('Desafio diario','Um treino curto para hoje.',`<section class="section challenge"><h2>${e.title}</h2><p>${e.description}</p><button class="primary" onclick="openExercise('${e.id}')">Comecar desafio</button></section>`)}function progress(){nav('progress');let s=stats();app.innerHTML=shell('Seu progresso','Acompanhe sua evolucao.',`<div class="cards"><div class="card"><div class="label">Resolvidos</div><div class="value">${s.solved}</div></div><div class="card"><div class="label">Taxa de acerto</div><div class="value">${s.accuracy}%</div></div></div><section class="section"><h2>Historico</h2>${s.attempts.length?s.attempts.map(a=>`<div class="result ${a.passed?'ok':'fail'}">${a.passed?'Resolvido':'Nova tentativa'} ? ${a.category}</div>`).join(''):'<p>Seu historico aparecera apos o primeiro envio.</p>'}</section>`)}function achievements(){nav('achievements');let s=stats();app.innerHTML=shell('Conquistas','Marcos da sua jornada.',`<div class="achievement-grid"><div class="achievement ${s.solved?'':'locked'}"><span class="icon">1</span><b>Primeiro exercicio</b><p>${s.solved?'Conquistada!':'Continue treinando'}</p></div><div class="achievement ${s.solved>=4?'':'locked'}"><span class="icon">4</span><b>Quatro exercicios</b><p>${s.solved>=4?'Conquistada!':'Continue treinando'}</p></div></div>`)}function settings(){nav('settings');app.innerHTML=shell('Configuracoes','Preferencias da experiencia.',`<section class="section"><h2>Linguagem de treino</h2><p>JavaScript esta ativa. O codigo roda no navegador, sem servidor.</p><div class="hint">O progresso fica salvo somente neste navegador.</div></section>`)}document.querySelectorAll('nav button').forEach(b=>b.onclick=()=>({home,train,challenges,progress,achievements,settings}[b.dataset.view]()));home();
